asyncapi: 2.0.0
info:
  title: Boolean
  version: "1.0.0"

components:
  schemas:
    in-record:
      type: object
      required: [required]
      properties:
        required:
          type: boolean
        unrequired:
          type: boolean
    as-proxy:
      type: boolean
